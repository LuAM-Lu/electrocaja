# =======================
# 🌐 Configuración Server
# =======================

# Puerto del backend
PORT=3001
NODE_ENV=development

# Base de datos PostgreSQL
# Formato: postgresql://usuario:clave@host:puerto/basededatos
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/electro_caja

# JWT (seguridad)
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=8h

# Host de escucha (0.0.0.0 permite red local)
HOST=0.0.0.0

# URL del cliente permitido
CLIENT_URL=http://localhost:5173

# IP/URL en red local (cambia a tu IP real si usas varias PCs)
LOCAL_IP=192.168.1.x
NETWORK_URL=http://192.168.1.x:3001

# CORS (separadas por comas)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Socket.IO (separadas por comas)
SOCKET_CORS_ORIGINS=http://localhost:5173

# Configuración Email (usa datos falsos en dev)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=user@example.com
EMAIL_PASS=password
